pipeline {
  agent any
  stages {
    stage('error') {
      steps {
        git(url: 'git@github.com:xyhui/demogit.git', branch: 'master')
        git(url: 'git@github.com:xyhui/demogit.git', branch: 'master')
      }
    }
  }
}